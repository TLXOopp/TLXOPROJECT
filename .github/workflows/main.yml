name: Update Game Data

permissions:
  contents: write # [중요] 로봇에게 쓰기 권한 부여

on:
  schedule:
    - cron: '0 * * * *' # 1시간마다 실행
  workflow_dispatch: # 수동 실행 버튼 활성화

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: 코드 가져오기
        uses: actions/checkout@v3

      - name: Node.js 설치
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 데이터 수집 실행
        run: node updater.js

      - name: 결과 저장 (자동 커밋)
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "🎮 최신 스팀 데이터 업데이트"
          file_pattern: games.json